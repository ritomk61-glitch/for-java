import java.util.*;
class P{
    public static void main(String[] args){

        //input

        // nextLine use when we need to print whole line not only the word then we use it

        // nextint()
        // nextfloat()......aivabe tmi seperate seperate input nite parbe

        Scanner sc = new Scanner(System.in); 
        String name = sc.nextLine();
        System.out.println(name); 
        sc.close();     
          }
 
}